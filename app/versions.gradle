ext {
  VERSION_MAJOR = 2
  VERSION_MINOR = 1
  VERSION_PATCH = 2
  VERSION_NAME = "${VERSION_MAJOR}.${VERSION_MINOR}.${VERSION_PATCH}"
  VERSION_CODE = VERSION_MAJOR * 1000 + VERSION_MINOR * 100 + VERSION_PATCH
}